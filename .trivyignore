# Accept these; revisit as needed

# In python: this only applies to 3.8 which is bundled for ease of testing and
# in case someone wants to use the older isyntax reader.  It is not directly
# exposed

# The other python issue can only be reached if a developer installs an unsafe
# tarball via setuptools; it has not direct exposure

# The ubuntu issues cannot be reached

# HIGH: python-pkg - setuptools: Path Traversal Vulnerability in setuptools Pac
CVE-2025-47273
# HIGH: python-pkg - jaraco.context: jaraco.context: Path traversal via malicio
CVE-2026-23949
# HIGH: python-pkg - wheel: wheel: Privilege Escalation or Arbitrary Code Execu
CVE-2026-24049
# HIGH: python-pkg - pillow: Pillow: Out-of-bounds Write via Specially Crafted
CVE-2026-25990
# HIGH: ubuntu - kernel: kernel: Privilege escalation or denial of service via
CVE-2026-23231
